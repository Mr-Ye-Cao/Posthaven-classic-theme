<div class="post-responses">
  {{ post | share_buttons }}
  <a
    href="https://x.com/intent/post?original_referer={{ site.url | url_encode }}&ref_src=twsrc%5Etfw%7Ctwcamp%5Ebuttonembed%7Ctwterm%5Eshare%7Ctwgr%5E&text={{ post.title | url_encode }}&url={{ post.url | url_encode }}"
    target="_blank"
    class="subscribe-button post-share-button"
    style="background-color: black; color: white; border-color: black; text-decoration: none !important;"
  >
    Post
  </a>

  {% if site.comments_enabled? or post.response_count > 0 %}
    <div class="count">
      <span class="comment-icon"></span>
      {{ post.response_count | pluralize: 'response' }}
    </div>
    <div class="responses">
      {% for response in post.responses %}
        {% include 'response' %}
      {% endfor %}
      {% if post.anon_upvote_count > 0 %}
        <div class="response">
          <div class="upvote">
            {{ post.anon_upvote_count | pluralize: 'visitor' }}
            upvoted this post.
          </div>
        </div>
      {% endif %}
    </div>
  {% endif %}
</div>
